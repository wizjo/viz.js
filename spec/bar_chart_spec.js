describe('BarChart', ['assert', '../src/lib/class', '../src/lib/chart', '../src/lib/bar_chart'], function(assert, Class, Chart) {
  before(function() {
    
  });
  
  describe('#addBar', function() {
    it('should add bar', function() {
      assert.equal(1, 1);
    });
  });
});